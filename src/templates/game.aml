let CANVAS_WIDTH=20
let CANVAS_HEIGHT=20

if visible
  if paused
    @Paused
  else
    vstack
      hstack
        spacer
        text title
        spacer
      hstack
        spacer
        vstack [width: CANVAS_WIDTH]
          @GameType
          @Statistics
        vstack
          @LineCount
          @GameArena [paused: paused]
        vstack [width: CANVAS_WIDTH]
          @ScoreBoard
          @NextPiece
        spacer


