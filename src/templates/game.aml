let CANVAS_WIDTH=40
let CANVAS_HEIGHT=40
let PIECE_WIDTH=6
let PIECE_HEIGHT=3
let THEME = {
  level_bg: "cyan",
  level_fg: "red",
}

if visible
  hstack
    spacer
    vstack [width: CANVAS_WIDTH]
      @GameType
      @Statistics
    vstack
      @LineCount
      @GameArena
    vstack [width: CANVAS_WIDTH]
      @ScoreBoard
      @NextPiece
    spacer


