let CANVAS_WIDTH=20
let CANVAS_HEIGHT=20
let THEME = {
  level_bg: "cyan",
  level_fg: "red",
}

if visible
  vstack
    hstack
      spacer
      text title
      spacer
    hstack
      spacer
      vstack [width: CANVAS_WIDTH]
        @GameType
        @Statistics
      vstack
        @LineCount
        @GameArena [paused: paused]
      vstack [width: CANVAS_WIDTH]
        @ScoreBoard
        @NextPiece
      spacer


