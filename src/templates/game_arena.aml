border [border_style: "thick"]
  vstack
    // TODO: It would be nice to use the CANVAS_WIDTH from the parent widget so it can be dynamic
    canvas [width: CANVAS_WIDTH, height: CANVAS_HEIGHT]
    vstack
      for entry in debug
        text entry
